spring:
  datasource:
    url: jdbc:tc:postgresql:16-alpine:///oficina_test
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true

oficina:
  security:
    jwt:
      secret: test-secret-test-secret-test-secret-test-secret
